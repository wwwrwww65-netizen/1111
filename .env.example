NODE_ENV=development
PORT=4000

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_TRPC_URL=http://localhost:4000/trpc

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/ecom_db?schema=public
DIRECT_URL=postgresql://user:password@localhost:5432/ecom_db?schema=public

# Authentication
JWT_SECRET=replace_me_with_a_strong_secret

# Stripe
STRIPE_SECRET_KEY=sk_test_replace_me

# SMTP (Mailhog/Maildev for local dev or real SMTP)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# Redis (optional if used later)
REDIS_URL=redis://localhost:6379

# MinIO / S3 (optional if used later)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false
