{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "files": [
    "base.json",
    "nextjs.json",
    "react-native.json"
  ],
  "scripts": {
    "build": "echo \"No build required for typescript-config\"",
    "lint": "echo \"No linting required for typescript-config\"",
    "test": "echo \"No tests required for typescript-config\"",
    "test:coverage": "echo \"No tests required for typescript-config\""
  }
}
