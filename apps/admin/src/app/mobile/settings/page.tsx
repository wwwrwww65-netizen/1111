export default function MobileSettings(): JSX.Element {
  return (
    <div className="grid" style={{ gap:12 }}>
      <div className="panel">
        <div style={{ fontWeight:700, marginBottom:8 }}>الإعدادات</div>
        <div style={{ color:'var(--sub)' }}>إعدادات سريعة للهاتف.</div>
      </div>
    </div>
  );
}

