// settings.gradle — minimal for pnpm monorepo, no Expo plugins

rootProject.name = 'E-commerce Mobile App'

// Include the app module
include ':app'

// If you have local RN modules you need as composite builds, يمكنك إضافتها هنا:
// includeBuild("../some-local-rn-module")

// IMPORTANT: تأكد أنه لا يوجد أي `plugins { id("com.facebook.react.settings") ... }`
// في أي مكان آخر من ملفات settings، لأننا نعتمد على classpath approach في build.gradle.
